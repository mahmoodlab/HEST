import torch.nn as nn

class CLIPVisionModelPostProcessor(nn.Module):
    def __init__(self, model):
        super().__init__()
        self.model = model

    def forward(self, x):
        out = self.model(x)
        return out.pooler_output