version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"
  apt_packages:
    - libvips
    - libvips-dev
    - openslide-tools

  jobs:
    post_checkout:
      - ln -s ../../tutorials docs/source/tutorials
      - ls docs/source
      - ls docs/source/tutorials

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

sphinx:
  configuration: docs/source/conf.py
